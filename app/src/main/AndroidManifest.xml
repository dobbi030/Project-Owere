<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owere">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Owere">
        <activity android:name=".activity.home.HomeActivity"></activity>
        <activity android:name=".activity.loginActivity.LoginActivity_temp" />
        <activity android:name=".activity.loginActivity.LoginActivity_choice" />
        <activity android:name=".activity.signUpActivity.SignUpActivity1_choice" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>